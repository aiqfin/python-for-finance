- 每个 Notebook 以 markdown 单元格开头，包含 O'Reilly 书籍封面、作者信息与 tpq.io 品牌标识作为统一模板。
- 数值计算统一使用 numpy 向量化操作（如 np.random.standard_normal、np.maximum、np.exp），避免 Python 原生循环。
- 绘图统一通过 plt.style.use('ggplot') 并设置 mpl.rcParams['font.family'] = 'serif' 保持视觉一致性。
- BSM 相关函数采用 NumPy/SciPy 标准文档字符串格式，参数与返回值用 ========== 分隔符明确标注类型与含义。