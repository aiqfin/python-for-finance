- 导入第三方库统一使用约定俗成的别名：`import numpy as np`、`import pandas as pd`、`import matplotlib.pyplot as plt`，并在每个notebook首行cell中声明。
- 每个notebook以markdown单元格开头，包含标题、来源说明（改编自《Python金融大数据分析（第2版）》相应章节）和使用说明。
- 金融数据建模遵循"行=日期，列=资产"的DataFrame约定，价格序列通过`pd.date_range`生成交易日索引。
- 绘图统一采用面向对象API：先`fig, ax = plt.subplots()`创建画布，再用`ax.plot`/`ax.set_title`等方法装饰，最后通过`savefig`导出高清图而非截图。