# 项目约定

## 教学风格

- 面向金融专业学生，用中文循序渐进讲解，按“问题—概念与代码—输出解释—练习”展开，不先堆结论或 API。
- 基础概念先用简短、可手算的非金融例子，再迁移到金融应用；章内尽量沿用连贯案例，字段与参数在用到时介绍。
- 代码保持直观、可逐步运行，减少不必要的封装和防御性错误捕捉；说明数据每行含义、单位及结果适用范围。
- 练习与参考答案分开，沿用已有折叠答案形式。

## 材料与目录

- 沿用各章现有结构与 LaTeX 主题；Notebook 与课件中的代码、图表和解释保持一致，教学页面集中在本章主 `.tex` 文件。
- 输入数据放本章 `data/`，运行产物放 `outputs/`，课件插图放 `latex/figures/`；临时代码与数据统一放项目根目录 `tmp/`。
- 优先使用本地教学数据，注明来源；Notebook 应能从头按顺序运行，路径沿用现有跨平台写法，避免个人绝对路径。

## 执行与修改

- 必须优先使用项目 `.venv` 中的 Python；安装依赖前先列出所需依赖并征得同意。
- 修改前查看目标文件及 Git 状态、差异；只改当前任务相关内容，不回滚或覆盖不属于当前线程的修改。
- 禁止哈希校验和 SHA 校验。
- 按改动范围验证：代码检查相关示例，课件编译并检查页面排版；不无故重跑整套 Notebook，交付时说明验证情况。
